// import { getFromCacheOrApi } from 'Base'

const onGet = async ({
    params,
    response,
}) => {

    // const data = await getFromCacheOrApi('/blog/data')

    // const { pageNumber, slug, key } = params

    // console.log(params, response.status)
    // if (pageNumber && isNaN(pageNumber)) {
    //     console.log(pageNumber, isNaN(pageNumber))
    //     response.status = 400
    //     return
    // }

    return {
        // ...data
    }
}

export { onGet }
